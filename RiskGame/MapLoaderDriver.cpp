#include "stdafx.h"
#include "MapLoader.h"

/* int main() {
 	Map map = read_map_file("World2.map");
 	 std::cout << std::endl;

	map.print_all_edges(map.get_graph());
	map.print_all_vertices(map.get_graph());
	std::cout << "Continents: ";
	for (auto continent : map.get_continents()) {
		std::cout << continent << " ";
	}
	std::cout << std::endl;
	std::cout << "--------------------" << std::endl;

 	 Vertex v = map.find_country_vertex("Alaska");
 	 map.traverse_edges(v);


 	 int x = 0;
 	 std::cin >> x;
 	 return 0;
 }
 */